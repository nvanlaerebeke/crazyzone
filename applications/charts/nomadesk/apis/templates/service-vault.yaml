apiVersion: v1
kind: Service
metadata:
  labels:
    app: nomadesk-vault
  name: nomadesk-vault
spec:
  selector:
    app: nomadesk-vault
  type: ClusterIP
  ports:
    - name: vault
      port: 80
      targetPort: 80

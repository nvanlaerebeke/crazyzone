apiVersion: v1
kind: Service
metadata:
  labels:
    app: nomadesk-link
  name: nomadesk-link
spec:
  selector:
    app: nomadesk-link
  type: ClusterIP
  ports:
    - name: link
      port: 80
      targetPort: 80

apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ $.Chart.Name | lower }}-storage-mosquitto
  labels:
    app: {{ $.Chart.Name | lower }}-storage-mosquitto
spec:
  storageClassName: standard
  capacity:
    storage: 50Gi
  accessModes:
  - ReadWriteOnce
  nfs:
    server: nas.crazyzone.be
    path: "/volume1/docker-storage/home/mosquitto"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $.Chart.Name | lower }}-storage-mosquitto-claim
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  selector:
    matchLabels:
      app: {{ $.Chart.Name | lower }}-storage-mosquitto
  resources:
    requests:
      storage: 50Gi
  volumeMode: Filesystem

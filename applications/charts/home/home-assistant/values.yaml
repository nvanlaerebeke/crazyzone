home-assistent:
  env:
    TZ: CET
  persistence:
    config:
      enabled: true
  postgresql:
    enabled: true
    primary:
      persistence:
        primary:
          enabled: true
          existingClaim: home-assistant-claim
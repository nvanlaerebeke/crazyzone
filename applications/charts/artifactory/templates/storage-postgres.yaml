apiVersion: v1
kind: PersistentVolume
metadata:
  name: artifactory-postgres-storage-name
  namespace: operations
  labels:
    app: artifactory-postgres-storage
spec:
  storageClassName: standard
  capacity:
    storage: 50Gi
  accessModes:
  - ReadWriteOnce
  nfs:
    server: nas.crazyzone.be
    path: "/volume1/docker-storage/artifactory/artifactory"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: artifactory-postgres-storage-claim
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  selector:
    matchLabels:
      app: artifactory-postgres-storage
  resources:
    requests:
      storage: 50Gi
  volumeMode: Filesystem
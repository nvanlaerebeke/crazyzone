apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: 'gitlab'
spec:
  project: 'default'
  destination:
    name: ''
    namespace: 'gitlab'
    server: 'https://kubernetes.default.svc'
  source:
    path: 'applications/charts/gitlab'
    repoURL: '$GIT_URL'
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true